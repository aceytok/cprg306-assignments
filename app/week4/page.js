"use client";
import NewItem from "./new-item.js";

export default function Page() {
    return (
        <main class="flex justify-center w-full bg-gradient-to-t from-blue-100 via-blue-300 to-blue-500">
            <NewItem />
        </main>
    )
}